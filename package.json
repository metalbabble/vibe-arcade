{
  "name": "vibe-arcade",
  "private": true,
  "workspaces": ["games/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present && cp index.html dist/",
    "serve": "npx serve dist -p 8080"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  }
}
